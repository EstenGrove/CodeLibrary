import React from "react";
import styles from "../css/views/ColorConverterView.module.scss";
import { PropTypes } from "prop-types";

const ColorConverterView = () => {
	return (
		<div className={styles.ColorConverterView}>
			{/*  */}
			{/*  */}
		</div>
	);
};

export default ColorConverterView;

ColorConverterView.defaultProps = {};

ColorConverterView.propTypes = {};
