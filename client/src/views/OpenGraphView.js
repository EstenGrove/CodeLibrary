import React from "react";
import styles from "../css/views/OpenGraphView.module.scss";
import { PropTypes } from "prop-types";

const OpenGraphView = () => {
	return (
		<div className={styles.OpenGraphView}>
			{/*  */}
			{/*  */}
		</div>
	);
};

export default OpenGraphView;

OpenGraphView.defaultProps = {};

OpenGraphView.propTypes = {};
