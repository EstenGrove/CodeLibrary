import React from "react";
import styles from "../css/views/ParserView.module.scss";
import { PropTypes } from "prop-types";

const ParserView = () => {
	return (
		<div className={styles.ParserView}>
			{/*  */}
			{/*  */}
			{/*  */}
		</div>
	);
};

export default ParserView;

ParserView.defaultProps = {};

ParserView.propTypes = {};
